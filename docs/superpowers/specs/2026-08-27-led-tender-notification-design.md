# LED 입찰 공고 캘린더 및 메일 알림 설계

## 1. 목적

회사 홈페이지 어드민에서 LED 조명 관련 입찰 공고를 등록일 기준 월간 캘린더로 조회하고, 여러 회사 이메일 주소로 하루 한 번 신규 공고 요약 메일을 발송한다. 직접적인 조명 공고뿐 아니라 전기공사·시설개선처럼 조명이 포함될 가능성이 있는 공고도 수집하되, 관련도를 명확히 구분한다.

## 2. 현재 프로젝트 기준

- 프론트엔드: `led-lighting-website`, Nuxt 4, Vue 3, TypeScript, Element Plus, Tailwind CSS
- 백엔드: `dfkorea-backend`, NestJS 10, TypeORM, PostgreSQL, `node-cron`
- 인증: 기존 관리자 JWT 인증과 `JwtAuthGuard` 재사용
- 배포: 기존 문서 기준 Railway 백엔드와 PostgreSQL 사용
- 기존 어드민: `/admin/dashboard`의 좌측 탭 내비게이션과 청록색 `#22a8c3` 포인트 컬러 유지

## 3. 범위

### 3.1 포함

- 공식 API가 제공되는 출처의 입찰 공고 수집
- 출처별 응답을 하나의 공고 형식으로 정규화
- 직접 관련 및 잠재 관련 자동 분류와 판정 근거 저장
- 등록일 기준 월 캘린더 및 일자별 공고 목록
- 캘린더 조회 전용 검색·필터
- 여러 수신 이메일과 공통 발송 시각 설정
- 신규 공고 일일 요약 메일과 실패 시 1회 재시도
- 수집·메일 실행 이력 및 중복 수집·발송 방지

### 3.2 제외

- 마감 임박 알림
- 수신 주소별 개별 발송 시각
- 이메일용 검색어·지역·출처·공고 유형 필터
- 공식 API 또는 정식 데이터 제휴가 없는 사이트의 HTML 크롤링
- 입찰 참여, 투찰, 낙찰 예측 기능
- AI 기반 분류 및 첨부 문서 본문 OCR

## 4. 초기 데이터 출처

### 4.1 조달청 나라장터

- 공식 API: [조달청 나라장터 입찰공고정보서비스](https://www.data.go.kr/data/15129394/openapi.do)
- 대상: 물품, 공사, 용역 공고
- 주요 정보: 공고번호와 차수, 공고명, 발주·수요기관, 등록일, 입찰 기간, 지역, 금액, 원문 링크

### 4.2 국토교통부 K-apt

- 공식 API: [공동주택 입찰공고 정보제공 서비스](https://www.data.go.kr/data/15058166/openapi.do)
- 대상: 아파트 공사·용역·물품 공고 및 K-apt에 연계된 민간 전자입찰 정보
- 주요 정보: 단지명, 공고명, 공고일, 마감일, 입찰 유형, 지역, 원문 링크

### 4.3 한국전력공사 전자입찰

- 공식 API: [한국전력공사 전자입찰계약정보](https://www.data.go.kr/data/15148223/openapi.do)
- 대상: 한전 및 전력그룹사 전자입찰 공고
- 주요 정보: 공고번호, 공고명, 발주기관, 계약·낙찰 방식, 공고·투찰 일정, 금액

### 4.4 후속 출처 추가 기준

발전사·공공기업 등 신규 출처는 현재 운영되는 공식 REST API, 인증키 발급 절차, 이용 허락 범위, 호출 한도, LED 관련 공고의 실효성을 확인한 경우에만 같은 어댑터 계약으로 추가한다. 나라장터에 이미 포함된 공고는 원본 공고번호와 기관·일정 비교로 중복을 제거한다.

## 5. 아키텍처

기존 NestJS 애플리케이션에 `tenders` 도메인을 추가한다. 출처 어댑터와 예약 작업 실행부를 분리하며, 현재 배포부터 여러 백엔드 replica가 동시에 실행될 수 있다고 가정한다. 예약 작업은 같은 PostgreSQL 세션에서 유지되는 advisory lock과 영속 고유 claim을 함께 사용한다.

### 5.1 백엔드 책임

- `TenderSourceAdapter`: 출처별 API 호출과 페이지네이션, 원본 응답 반환
- `TenderNormalizer`: 출처별 응답을 공통 `NormalizedTender`로 변환
- `TenderClassifier`: 직접·잠재 관련 점수와 판정 근거 계산
- `TenderIngestionService`: 정규화, 분류, 중복 확인, 신규 저장 및 변경 갱신
- `TenderQueryService`: 월별 집계, 일자별 목록, 상세 조회
- `TenderSubscriptionService`: 수신 사용 여부, 주소 목록, 발송 시각 관리
- `TenderMailService`: 이메일 HTML 생성과 네이버웍스 SMTP 발송
- `TenderSchedulerService`: 2회 수집, 일일 메일, 실패 재시도 예약

### 5.2 스케줄

- 공고 수집: 매일 `00:00`, `12:00`, `Asia/Seoul`
- 메일 발송: 매분 공용 설정의 enabled·발송 시각·활성 수신자를 DB에서 다시 읽고, 현재 KST 시각이 저장 시각 이상이면 KST 영업일 고유 claim을 획득한 한 replica만 실행
- 메일 재시도: 특정 주소의 1차 발송 실패 10분 뒤 해당 주소만 1회
- 재시도 실패: 추가 자동 재시도 없이 실패 기록. 다음 날 해당 주소의 메일에 미발송 공고 재포함

수집, 일일 메일, 재시도는 각각 전용 PostgreSQL advisory lock을 QueryRunner의 같은 연결 세션에서 획득·해제한다. 일일 메일은 추가로 `tender_daily_dispatches.businessDate` 고유 제약을 최종 idempotency 경계로 사용하므로 replica 중복 실행과 당일 발송 시각 변경에도 하루 한 번만 claim된다. 설정 PUT은 process-local cron을 재예약하지 않으며 다음 minute tick부터 모든 replica에 보인다. 수집 조회 시작은 출처별 마지막 정상 수집 시각의 1시간 전으로 잡고 DB 고유 키로 중복을 제거한다. 최초 수집은 실행 시각 이전 24시간을 조회한다.

## 6. 관련도 분류

### 6.1 직접 관련

공고명, 품목명, 상세 설명 또는 첨부파일명에서 LED, 조명, 등기구, 가로등, 보안등, 투광등, 경관조명, 실내조명, 조명제어 등 직접 키워드를 발견한 공고다. UI와 메일에서 `💡 직접 관련` 청록색 배지를 사용한다.

### 6.2 잠재 관련

전기공사, 전기설비, 시설개선, 리모델링, 에너지 절감, 주차장 개선, 스마트시티 등 조명 공급이 포함될 가능성이 있는 간접 키워드를 발견한 공고다. UI와 메일에서 `⚡ 잠재 관련` 황색 배지를 사용한다.

### 6.3 판정 원칙

- 규칙별 가중치를 합산하고 직접 관련 임계값을 우선 적용한다.
- 직접 키워드와 잠재 키워드가 함께 있으면 직접 관련으로 분류한다.
- 판정에 사용된 필드, 키워드, 점수를 `relevanceReasons`에 저장한다.
- 어떤 관련 키워드도 없는 공고는 저장하지 않고 수집 실행의 제외 건수에만 반영한다.
- 분류 규칙은 코드와 테스트에 명시적으로 관리하며 UI에서 편집하는 기능은 초기 범위에 넣지 않는다.

## 7. 데이터 모델

### 7.1 `tenders`

- UUID 기본 키
- 출처 코드, 원본 공고번호, 공고 차수
- 공고명, 발주기관, 수요기관
- 등록일시, 입찰 시작·마감·개찰 일시
- 지역, 공고 유형, 계약 방식, 추정 금액
- 공식 원문 URL
- 관련도 `DIRECT | POTENTIAL`, 관련 점수, 판정 근거 JSON
- 출처 원본 응답 JSONB
- 최초 수집·마지막 갱신 일시
- `source + sourceNoticeId + revision` 고유 제약

### 7.2 `tender_subscriptions`

- 단일 공통 구독 설정
- 사용 여부
- `HH:mm` 발송 시각
- 고정 시간대 `Asia/Seoul`
- 생성·수정 일시

### 7.3 `tender_recipients`

- 구독 설정 외래 키
- 정규화된 이메일 주소
- 이메일 주소 고유 제약
- `isActive` soft activation. 설정에서 제거해도 행과 메일 이력을 삭제하지 않고, 다시 추가하면 같은 ID를 활성화
- 생성 일시

### 7.4 `tender_sync_runs`

- 출처 코드, 예정·시작·종료 시각
- `RUNNING | SUCCEEDED | FAILED` 상태
- 조회, 신규, 변경, 제외 건수
- 실패 오류 코드와 안전하게 정리된 오류 메시지

### 7.5 `tender_mail_deliveries`

- 대상 수신 주소, 대상 기간, 시도 횟수 `1 | 2`
- `PENDING | SENT | FAILED | RETRY_SCHEDULED | SKIPPED | CANCELLED | DELIVERY_UNCERTAIN` 상태
- SMTP 메시지 ID, durable `claimedAt`, 성공·실패·불확실 시각, 안전하게 정리된 오류
- stale in-flight SMTP 결과는 terminal `DELIVERY_UNCERTAIN`이며 재시도하지 않음

### 7.6 `tender_mail_items`

- 수신 주소와 공고의 발송 상태 `PENDING | SENT | DELIVERY_UNCERTAIN`
- 마지막으로 시도한 메일 발송 외래 키와 성공·불확실 시각
- `recipientId + tenderId` 고유 제약
- 실패 시 `PENDING`을 유지하고 다음 재시도 또는 다음 날 발송에 다시 포함

### 7.7 `tender_daily_dispatches`

- KST `businessDate` 고유 제약: 여러 replica와 당일 발송 시각 변경을 통틀어 하나의 일일 실행 identity
- claim 시 관측한 공용 `deliveryTime`
- `CLAIMED | COMPLETED` 상태와 claim·완료·생성·수정 시각
- advisory lock과 별도로 DB unique insert가 최종 중복 방지 경계

DB 변경 구현과 같은 작업에서 레포 루트의 `database-schema.md`를 새로 만들고 테이블, 관계, 고유 제약, 인덱스를 최신 상태로 기록한다.

## 8. API 설계

모든 엔드포인트는 기존 `JwtAuthGuard`로 보호한다.

- `GET /tenders/calendar?month=YYYY-MM&keyword&source&region&procurementType&relevance`: 월의 날짜별 전체·직접·잠재 건수. 목록과 같은 조회 전용 필터를 적용하며, 이메일 수신 대상에는 영향을 주지 않는다.
- `GET /tenders`: `registeredDate`, `keyword`, `source`, `region`, `procurementType`, `relevance`, `page`, `pageSize`로 목록 조회
- `GET /tenders/:id`: 상세 공고와 판정 근거 조회
- `GET /tenders/subscription`: 수신 사용 여부, 이메일 목록, 발송 시각 조회
- `PUT /tenders/subscription`: 전체 수신 설정 원자적 저장

조회 API는 등록일을 기준으로 캘린더와 목록을 일치시킨다. 목록 기본 정렬은 등록일시 내림차순이다.

## 9. 어드민 화면 설계

### 9.1 내비게이션

기존 좌측 어드민 메뉴에 `입찰 공고`를 추가한다. 기존 제품·소식·인증서·설정과 같은 탭 패턴을 유지한다.

### 9.2 월 캘린더

- 7열 × 6주, 총 42개 날짜 셀을 항상 표시
- 현재 월 밖의 날짜는 흐린 색으로 표시
- 상단 요약 카드: 이번 달 전체, 직접 관련, 잠재 관련 건수
- 각 날짜 셀: 직접·잠재 관련 건수를 배지로 분리
- 날짜 선택: 캘린더 아래에 해당 등록일의 공고 목록 표시
- 월 이동: 이전·다음 월 버튼

### 9.3 공고 목록과 상세

- 배지, 공고명, 발주기관, 출처, 지역, 등록일, 마감일, 금액 표시
- 판정 근거를 보조 문구로 표시
- 공고 선택 시 상세 모달 또는 드로어에서 전체 정보와 공식 원문 링크 제공
- 공식 원문은 새 탭으로 연다.

### 9.4 캘린더 필터

`필터` 버튼은 캘린더와 목록 조회에만 적용되는 패널을 연다.

- 공고명·발주기관 검색어
- 출처
- 지역
- 공고 유형
- 직접·잠재 관련도
- 적용과 초기화

이 필터는 이메일 발송 대상을 변경하지 않는다.

### 9.5 수신 설정

`수신 설정` 버튼은 다음 항목만 제공한다.

- 메일 수신 사용 여부
- 여러 수신 이메일 주소 추가·삭제
- 공통 일일 발송 시각
- 저장과 취소

모달을 열 때마다 새 GET 요청을 시작한다. 단조 증가 request generation을 사용해 가장 최신 요청만 설정·loaded·loading·error 상태를 바꿀 수 있고, 닫기는 진행 중 세대를 무효화한다. 최신 조회가 실패하면 이전에 성공한 설정은 읽기 전용으로 보존하되 저장을 비활성화하고 실제 `다시 시도` 동작을 제공한다.

메일 대상은 캘린더 필터와 무관하게 새로 수집된 모든 직접·잠재 관련 공고다.

### 9.6 반응형

- 데스크톱: 월 전체 캘린더와 하단 목록
- 태블릿: 동일 구조를 유지하고 셀 내부는 건수 중심으로 축약
- 모바일: 요일 7열은 유지하되 날짜 셀에 아이콘별 숫자만 표시하고, 선택 날짜 목록은 단일 열 카드로 표시

## 10. 이메일 설계

### 10.1 발송 방식

- SMTP 호스트: `smtp.worksmobile.com`
- 포트: `465` SSL
- 사용자 ID: 도메인을 포함한 네이버웍스 이메일 주소
- 비밀번호: 네이버웍스 외부 앱 비밀번호
- 각 수신 주소에 개별 발송해 주소 상호 노출 방지

네이버웍스 관리자가 SMTP 사용을 허용해야 하며, 발신 계정의 외부 앱 비밀번호를 별도로 발급한다. 자격 증명은 환경 변수에만 저장한다.

### 10.2 발송 대상

- 해당 수신 주소에 성공 발송된 적 없는 공고
- 수집 시각이나 캘린더 조회 필터와 무관
- 직접 관련과 잠재 관련 모두 포함
- 발송할 신규 공고가 없으면 메일을 보내지 않고 `SKIPPED` 이력을 기록

### 10.3 본문

- 제목: `[DF KOREA 입찰정보] YYYY-MM-DD 신규 공고 N건`
- 상단: 전체·직접·잠재 건수
- 직접 관련 섹션과 잠재 관련 섹션 분리
- 각 공고: 공고명, 발주기관, 출처, 등록일, 마감일, 지역, 유형, 금액, 판정 근거, 공식 원문 링크
- HTML을 기본으로 하고 텍스트 대체 본문도 함께 생성

### 10.4 실패 처리

- 주소별 SMTP 결과를 기록한다.
- 확인된 1차 SMTP 실패 주소만 10분 뒤 한 번 재시도한다.
- SMTP 성공 응답 뒤에만 해당 주소와 공고 조합을 성공 발송으로 확정한다.
- 재시도 실패 공고는 다음 날 해당 주소의 대상 선정에 다시 포함한다.
- SMTP 승인과 그 뒤 DB commit은 원자화할 수 없다. in-flight claim이 stale이면 SMTP가 이미 승인했을 수 있으므로 delivery와 item을 terminal `DELIVERY_UNCERTAIN`으로 표시하고 재전송하지 않는다. 이는 이미 승인된 주소의 중복을 우선 방지하며, 극히 드문 메일 누락 가능성을 명시적으로 감수하는 절충이다.
- stale 전이와 늦게 도착한 확인된 실패는 조건부 lease update로 경합하므로 `DELIVERY_UNCERTAIN`이 retry 상태로 되살아나지 않는다. 늦은 SMTP 성공은 실제 승인을 `SENT`로 확정할 수 있다.

## 11. 오류 및 보안

- 출처 하나가 실패해도 다른 출처의 수집은 계속한다.
- 수집 실패 출처는 다음 `00:00` 또는 `12:00` 실행에서 겹치는 기간으로 재조회한다.
- 외부 API 원본 오류 응답과 인증키는 사용자 응답이나 일반 로그에 노출하지 않는다.
- API 키, 네이버웍스 계정, 외부 앱 비밀번호는 백엔드 환경 변수에서만 읽는다.
- 이메일 형식, 중복 주소, 최대 수신자 20개, `HH:mm` 시각을 DTO에서 검증한다.
- 어드민 조회·설정 API는 모두 JWT 인증을 요구한다.
- 달력·목록 API 실패 시 기존 데이터를 성공 응답처럼 보이지 않고 오류 상태와 재시도 동작을 표시한다.

## 12. 테스트 전략

### 12.1 백엔드 단위 테스트

- 출처별 정상·빈 응답·페이지네이션·오류 정규화
- 직접 키워드, 잠재 키워드, 혼합 키워드, 무관 공고 분류
- 판정 근거 생성
- 고유 키 기반 중복 제거와 변경 공고 갱신
- 월별·일자별 등록일 집계
- 검색어·출처·지역·유형·관련도 필터 조합
- 수신 설정 이메일·시각 검증
- 수신 주소별 미발송 공고 선정
- 1차 실패 후 10분 재시도와 최대 2회 제한
- 성공 주소 제외 및 실패 주소 다음 날 재포함
- stale SMTP claim의 terminal uncertainty, 늦은 성공·실패 경합, 재선정 금지
- minute due check의 이전/새 발송 시각과 두 replica의 KST business-date 단일 claim

### 12.2 백엔드 통합 테스트

- JWT 없는 요청 거부
- 수집 → DB 저장 → 캘린더 조회 흐름
- 수신 설정 저장 → 예약 발송 → 이력 기록 흐름
- 부분 출처 실패에도 정상 출처 저장
- 복수 스케줄 실행 시 advisory lock으로 단일 실행

### 12.3 프론트엔드 검증

- 6주 전체 월 캘린더 렌더링
- 이전·다음 월 이동과 날짜 선택
- 직접·잠재 배지와 건수 일치
- 필터 적용·초기화 및 이메일 설정과의 독립성
- 이메일 추가·삭제·중복·형식 오류
- 수신 사용 여부와 발송 시각 저장
- 모달 재오픈·재시도와 out-of-order 구독 요청 generation 격리
- 로딩, 공고 없음, API 오류, 모바일 레이아웃

## 13. 운영 및 문서화

- 환경 변수 예제와 배포 문서에 공공데이터 API 키, 한전 API 키, SMTP 호스트·포트·계정·외부 앱 비밀번호, 시간대를 추가한다.
- `database-schema.md`를 생성하고 DB 변경 때마다 갱신한다.
- 신규 어드민 메뉴 기능 현황 문서 `docs/menus/tenders.md`를 만들고 `구현 완료`, `미구현`, `부족하거나 개선이 필요한 기능`, `관련 파일`, `갱신 규칙` 구성을 유지한다.
- 실제 API 승인 전 또는 mock 응답만 연결된 단계는 완료로만 표시하지 않고 운영 한계를 함께 기록한다.
- 수집 실행과 메일 발송 로그에 비밀번호, API 키, 전체 SMTP 응답 원문을 남기지 않는다.

## 14. 완료 기준

- 세 초기 출처의 공식 API에서 관련 공고가 `00:00`, `12:00` KST에 수집된다.
- 동일 공고와 차수가 중복 저장되지 않는다.
- 직접·잠재 관련 배지와 판정 근거가 캘린더, 목록, 메일에서 일치한다.
- 등록일 기준 월 전체 캘린더와 날짜별 목록이 동작한다.
- 캘린더 필터가 이메일 대상에 영향을 주지 않는다.
- 여러 주소와 공통 발송 시각을 저장할 수 있다.
- 모든 신규 관련 공고가 주소별로 하루 한 번만 발송된다.
- 특정 주소 실패 시 10분 뒤 한 번만 재시도하며 다른 주소의 성공 상태는 유지된다.
- 재시도 실패 공고가 다음 날 해당 주소 발송 대상에서 누락되지 않는다.
- SMTP 결과가 불확실한 stale claim은 `DELIVERY_UNCERTAIN`으로 종결되어 중복 재전송되지 않는다.
- 여러 replica와 설정 시각 변경에서도 KST 영업일별 일일 dispatch claim은 하나뿐이다.
- 인증, 분류, 중복 제거, 집계, 발송 재시도 핵심 테스트가 통과한다.
